---
label: Post
hide_body: false
fields:
- type: text
  name: layout
  label: layout
- type: text
  name: title
  label: title
  config:
    required: true
- type: datetime
  name: date
  label: date
  config:
    required: true
    date_format: DD/MM/YYYY
- type: text
  name: client
  label: client
  config:
    required: true
- name: project_page
  type: boolean
  label: Project page
  description: Toggle to on if this project needs it's own page
- name: category
  type: select
  default: film
  config:
    required: true
    options:
    - film
    - " fashion / photography"
    - " exhibitions / events"
    - " workshops"
    - " theatre"
    - puppets
    source:
      type: simple
      section: 
      file: 
      path: 
  label: category
pages:
- _posts/2019-03-06-some-project.md
